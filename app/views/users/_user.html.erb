<div class="flex items-center p-2">
  <!-- User Avatar Placeholder -->
  <div class="flex-shrink-0 w-10 h-10 bg-blue-600 text-white rounded-full flex items-center justify-center text-lg font-semibold">
    <%= user.name[0].upcase %>
  </div>
  <!-- User Name -->
  <div class="ml-4">
    <p class="text-sm font-medium text-gray-900 dark:text-gray-100">
      <%= user.name %>
    </p>
  </div>
</div>
